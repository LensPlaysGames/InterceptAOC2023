/*
#include <stdlib.h>

size_t udiv(size_t lhs, size_t rhs) {
  return lhs / rhs;
}
size_t umod(size_t lhs, size_t rhs) {
  return lhs % rhs;
}

long long sdiv(long long lhs, long long rhs) {
  return lhs / rhs;
}
long long smod(long long lhs, long long rhs) {
  return lhs % rhs;
}

*/

#include <stdio.h>
#include <stdlib.h>

void print_number(size_t number) {
  printf("%zu\n", number);
}
